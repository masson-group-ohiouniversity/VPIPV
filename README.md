py -m pip install --upgrade pip
py -m pip install streamlit numpy scipy plotly openpyxl
python3 -m pip install kaleido==0.2.1
py -m streamlit run VPIPV_v78.py
#version 89: fixed bug in the definition of S0 = G + GM + GM2
#version 96: global fitting; made with Claude.ai as chatgpt was unable to handle fitting and proper data entry in the boxes. Claude started with version 89.
